# RUN 001 — Real Nutrient DWS extraction

Executed at `2026-08-17T17:14:58Z`, inside the official DevNetwork 2026
development window.

## Operation

- Endpoint: `POST https://api.nutrient.io/extraction/extract`
- Provider: Nutrient DWS Data Extraction API
- Parse mode: `understand`
- Schema: `consent-v1`
- Citations: enabled
- Input: locally generated one-page synthetic PDF

## Verified result

- HTTP status: `200`
- Four required fields were returned with citation metadata.
- All four fields passed the source-grounding and confidence policy.
- Deterministic decision: `auto_eligible`.
- The first append-only audit event verifies successfully.
- Public receipt: `dws-extract.public.json`.

## Privacy boundary

The API key, raw provider response, and source document text are not committed.
The public receipt contains only hashes, field status, decision, request ID, and
completion time. The source PDF is generated from synthetic `.test` addresses
and is excluded from Git.

## Failed probe retained as engineering evidence

An initial call to the Processor `/build` endpoint returned HTTP `403` because
the configured key belongs to Data Extraction. The integration then followed
the current official Data Extraction guide and switched to
`/extraction/extract`. A first malformed schema probe returned HTTP `400`; the
unsupported `additionalProperties` keyword was removed, after which the real
request succeeded. Neither failed response was represented as a successful run.
